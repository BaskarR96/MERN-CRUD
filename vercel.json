{
    "version": 2,
    "builds": [
        {
            "src": "backend/**/*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
              "src": "/(.*)",
              "dest": "frontend/build/$1"
        }
  ],
}
